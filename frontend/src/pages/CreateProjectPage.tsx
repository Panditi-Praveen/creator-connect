import PagePlaceholder from '../components/PagePlaceholder'

export default function CreateProjectPage() {
  return (
    <PagePlaceholder
      title="Create project"
      description="Project creation UI is a later roadmap stage."
    />
  )
}
