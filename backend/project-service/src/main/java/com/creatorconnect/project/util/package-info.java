/**
 * Reusable, dependency-free utilities.
 *
 * <p>Reserved for shared helpers. The Project Service currently has no custom
 * validators — the package exists to keep the architecture uniform across all
 * CreatorConnect services.
 */
package com.creatorconnect.project.util;
