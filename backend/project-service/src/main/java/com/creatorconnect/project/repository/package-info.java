/**
 * Spring Data JPA repositories.
 *
 * <p>{@code ProjectRepository} provides CRUD access plus the derived queries
 * used by the project use cases (browse feed and per-user listing).
 */
package com.creatorconnect.project.repository;
