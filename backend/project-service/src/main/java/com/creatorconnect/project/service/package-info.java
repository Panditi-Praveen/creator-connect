/**
 * Business logic contracts.
 *
 * <p>{@code ProjectService} defines the project use cases; implementations live
 * in {@code service.impl}. The interface decouples controllers from concrete
 * logic (SOLID — dependency inversion).
 */
package com.creatorconnect.project.service;
