/**
 * Concrete implementations of the Hiring Service contracts.
 *
 * <p>{@code ApplicationServiceImpl} owns the application lifecycle: applying
 * (freelancer only, one application per project), viewing, deciding (creator
 * only), and withdrawing (application owner only).
 */
package com.creatorconnect.hiring.service.impl;
