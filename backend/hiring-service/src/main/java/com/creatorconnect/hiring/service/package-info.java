/**
 * Hiring Service business-logic contracts.
 *
 * <p>{@code ApplicationService} exposes the use cases behind the application
 * APIs. Implementations live in {@code service.impl}; the interface decouples
 * the controller from concrete logic (SOLID — dependency inversion).
 */
package com.creatorconnect.hiring.service;
