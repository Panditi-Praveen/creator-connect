/**
 * Configuration classes for the Hiring Service.
 *
 * <p>Contains the Spring Security filter chain ({@code SecurityBeansConfig}),
 * the shared JWT settings ({@code JwtProperties}), JPA auditing
 * ({@code JpaAuditingConfig}) and the OpenAPI / Swagger document
 * ({@code OpenApiConfig}).
 */
package com.creatorconnect.hiring.config;
