/**
 * Response DTOs returned by the Profile Service REST API.
 *
 * <p>{@code ApiResponse} is the generic success envelope, {@code ProfileResponse}
 * carries the profile projection, and {@code ErrorResponse} standardizes every
 * failure body produced by the global exception handler.
 */
package com.creatorconnect.profile.dto.response;
