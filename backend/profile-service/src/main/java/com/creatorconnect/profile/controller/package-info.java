/**
 * REST controllers.
 *
 * <p>Contains {@code ProfileController} — a thin, Swagger-documented layer
 * that delegates to the service interfaces and wraps every result in the
 * standard {@code ApiResponse} envelope. No business logic lives here.
 */
package com.creatorconnect.profile.controller;
