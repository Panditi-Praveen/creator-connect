/**
 * Spring Data JPA repositories.
 *
 * <p>Contains {@code ProfileRepository}, the data access layer for the
 * {@code Profile} aggregate. Queries are derived from method names; the
 * service layer owns all business rules and transaction boundaries.
 */
package com.creatorconnect.profile.repository;
