/**
 * Configuration classes for the Profile Service.
 *
 * <p>Contains JPA auditing, JWT property binding, the Spring Security filter
 * chain, and the OpenAPI document definition. Each class is a plain Spring
 * {@code @Configuration}/{@code @Component} bean — no business logic lives
 * here.
 */
package com.creatorconnect.profile.config;
