/**
 * Entity ↔ DTO converters.
 *
 * <p>Contains {@code ProfileMapper}, a hand-written component that owns all
 * mapping rules between the {@code Profile} entity and the request/response
 * DTOs. No mapping framework is used — the rules stay explicit and auditable.
 */
package com.creatorconnect.profile.mapper;
