/**
 * Service contracts (interfaces).
 *
 * <p>Each interface declares the use cases of the Profile Service; concrete
 * implementations live in {@code service.impl}. Controllers depend only on the
 * interfaces (SOLID — dependency inversion).
 */
package com.creatorconnect.profile.service;
