/**
 * Data Transfer Objects for the Profile Service REST API.
 *
 * <p>Request DTOs carry validated inbound payloads; response DTOs carry safe
 * outbound projections. DTOs are immutable-by-convention data carriers and
 * never leak JPA entities to clients.
 */
package com.creatorconnect.profile.dto;
