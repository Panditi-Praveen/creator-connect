/**
 * JPA entities mapped to the MySQL schema.
 *
 * <p>Contains the {@code Profile} aggregate — the creator/freelancer public
 * identity record. Entities use UUID primary keys, Lombok, Bean Validation,
 * and JPA auditing ({@code @CreatedDate} / {@code @LastModifiedDate}).
 */
package com.creatorconnect.profile.entity;
