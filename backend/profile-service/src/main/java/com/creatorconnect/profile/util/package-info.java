/**
 * Reusable, dependency-free utilities.
 *
 * <p>Contains the custom {@code @ValidUrl} Bean Validation constraint and its
 * {@code UrlValidator} implementation — a drop-in replacement for Hibernate
 * Validator's deprecated {@code @URL} annotation.
 */
package com.creatorconnect.profile.util;
