/**
 * Request DTOs consumed by the Auth Service REST API.
 *
 * <p>Each request DTO carries Jakarta Bean Validation annotations so invalid
 * payloads are rejected before reaching the service layer. DTOs are plain data
 * carriers — they are never JPA entities.
 */
package com.creatorconnect.auth.dto.request;
