/**
 * REST controllers exposing the Auth Service HTTP API.
 *
 * <p>Controllers are thin: they delegate business logic to the service layer,
 * map DTOs, and return proper HTTP status codes. Public endpoints: register,
 * login. Authenticated endpoints: me, logout.
 */
package com.creatorconnect.auth.controller;
