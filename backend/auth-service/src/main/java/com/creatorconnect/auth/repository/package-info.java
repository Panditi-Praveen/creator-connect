/**
 * Spring Data JPA repositories.
 *
 * <p>Provides data access for entities. {@code UserRepository} exposes derived
 * queries such as {@code findByEmail} and {@code existsByEmail}. Interfaces
 * only — no implementations are written by hand.
 */
package com.creatorconnect.auth.repository;
