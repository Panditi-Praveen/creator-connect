/**
 * Response DTOs returned by the Auth Service REST API.
 *
 * <p>{@code ApiResponse} is the generic success envelope, {@code RegisterResponse}
 * carries the registration result, {@code LoginResponse} carries the issued
 * JWT, and {@code ErrorResponse} standardizes every failure body produced by
 * the global exception handler.
 */
package com.creatorconnect.auth.dto.response;
