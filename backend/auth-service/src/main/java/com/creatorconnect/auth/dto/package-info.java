/**
 * Data Transfer Objects for the Auth Service API.
 *
 * <p>Request DTOs ({@code RegisterRequest}, {@code LoginRequest}) carry Jakarta
 * Bean Validation annotations; response DTOs ({@code AuthResponse},
 * {@code UserResponse}) define the API contract returned to clients. DTOs are
 * never JPA entities and entities are never exposed directly.
 */
package com.creatorconnect.auth.dto;
